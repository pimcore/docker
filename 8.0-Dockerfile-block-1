    apt-get install -y libavif-dev libheif-dev
    docker-php-ext-configure gd -enable-gd --with-freetype --with-jpeg --with-webp --with-avif --with-heic; \
    docker-php-ext-install gd; \
    pecl install -f xmlrpc; \
    pecl install imagick; \
